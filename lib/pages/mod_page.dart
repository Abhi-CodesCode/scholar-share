import 'package:flutter/material.dart';

class ModPage extends StatelessWidget {
  const ModPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Mod"),),
      body: Container(child: Text("Welcome"),),
    );
  }
}
